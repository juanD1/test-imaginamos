import React from 'react';
import { GlobalProps } from 'types/globalTypes';

const Content = ({ children }: GlobalProps) => (
  <div className="col col-md-8" style={{ backgroundColor: 'cyan' }}>
    hihihh
  </div>
);

export default Content;
